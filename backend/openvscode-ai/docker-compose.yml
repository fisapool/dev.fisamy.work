version: '3.8'

services:
  build:
    build:
      context: .
      dockerfile: Dockerfile
    image: openvscode-ai:latest
    container_name: openvscode-ai-builder
    command: echo "Image built successfully"
